pub async fn connect_to_command_center(device_id: &str) -> Result<(), Box<dyn std::error::Error>> {
    println!("[*] Bootstrapping Entity to boss.iz.life/api/register...");
    // let client = reqwest::Client::new();
    println!("[+] Successfully commanded link with Central Command.");
    Ok(())
}
